package com.cesg.upgrades;

/** Module: overflow of item types the box already stores is destroyed instead of rejected. */
public class VoidUpgradeItem extends ShulkerUpgradeItem {
    public VoidUpgradeItem(Properties properties) {
        super(properties);
    }
}
